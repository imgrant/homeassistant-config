# .gitignore file for Home Assistant config dir.
# An * ensures that everything will be ignored.
*

# Whitelist files/folders with !, these will not be ignored.
!.gitignore
!.github
!*.md
!.HA_VERSION
!esphome
!**/*.yaml
!**/*.yml

# Ignore folders.
.storage
.cloud
.google.token

# Ensure sensitive YAML files _are_ ignored.
ip_bans.yaml
**/secrets.yaml
known_devices.yaml
